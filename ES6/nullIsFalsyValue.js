var data=null
if(data)
{
    console.log("data is null");
}
else
{
    console.log("Data is not null");
}
//output : Data is not null

// data is already taken as null, if if test it again it will be assigned to not null 