var arr=[123.456,'hi']
var arr1=["suma", "kumuda"]
console.log(arr.push(arr1[0]));    // It will return the size of array after pushing 
console.log(arr.push("hiii"))
console.log(arr);               // & it will change original array

//This function can also push the whole array into the original array or.. we can push the single element by specifying the index